# Copie para .env.local para overrides locais (não commitado)
# Em desenvolvimento: deixe vazio — o proxy do Vite encaminha /v1/* para localhost:3000
# Em produção: defina a URL real do backend no painel do Render
VITE_API_URL=
