pre-commit:
  parallel: true
  commands:
    typecheck:
      run: bun tsc --noEmit
    lint:
      glob: "src/**/*.{ts,tsx}"
      run: bunx biome check --write --no-errors-on-unmatched {staged_files}
      stage_fixed: true

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
